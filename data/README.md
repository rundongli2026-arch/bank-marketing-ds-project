# Data Instructions

This project uses the public Kaggle dataset:
- `janiobachmann/bank-marketing-dataset`

The analysis scripts first try `kagglehub` auto-download.
If unavailable, place `bank.csv` in one of these locations:
- project root
- current working directory
- `data/raw/bank.csv`

Do not commit proprietary or sensitive data into this repository.
